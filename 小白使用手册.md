# Harness Suite 小白使用手册

> 适合第一次使用 Harness Suite 的同学。照着做就能跑通。

---

## 0. 先搞清楚一件事：命令在哪里执行？

`/harness:setup` 这类命令，**不是在系统终端（zsh/bash）里执行**。

要在 **Cursor/Claude 对话输入框**里执行，例如直接发：

```text
/harness:setup
```

如果你在终端里敲：

```bash
/harness:setup
```

会报：`no such file or directory`，这是正常现象。

---

## 1. 安装（推荐方式）

在你的业务项目根目录执行：

```bash
curl -fsSL "https://github.com/windchargerKang/my-harness-suite/archive/refs/heads/main.tar.gz" \
  | tar -xz && \
bash my-harness-suite-main/install.sh --target "$(pwd)" && \
rm -rf my-harness-suite-main
```

安装成功后：
1. 重启 Cursor/Claude 会话
2. 在对话框输入 `/harness:setup`

---

## 2. 一图看懂完整流程

1. `/harness:setup`：初始化规范
2. `/harness:propose <需求名>`：创建需求提案
3. `/harness:plan <change-id>`：做技术方案与任务拆解
4. `/harness:apply <change-id>`：按任务实现
5. `/harness:review <change-id>`：做并行评审
6. `/harness:archive <change-id>`：归档完成的变更
7. `/harness:knowledge`：管理知识沉淀

---

## 3. 每个命令怎么用（最简版）

## `/harness:setup`

**作用**：初始化项目，生成 OpenSpec 与 docs 骨架。

**你会得到**：
- `openspec/changes/`、`openspec/specs/`
- `docs/architecture/`、`docs/product/`、`docs/standards/`

**什么时候用**：项目第一次接入 Harness 时。

---

## `/harness:propose <需求名称>`

**作用**：创建需求提案。

**示例**：

```text
/harness:propose 用户登录功能
```

**你会得到**：
- `openspec/changes/<change-id>/proposal.md`

**下一步**：执行 `/harness:plan <change-id>`。

---

## `/harness:plan <change-id>`

**作用**：基于 proposal 做方案设计和任务拆解。

**示例**：

```text
/harness:plan user-login-20260410-01
```

**你会得到**：
- `design.md`（技术方案）
- `tasks.md`（任务清单）

---

## `/harness:apply <change-id>`

**作用**：按 `tasks.md` 执行实现。

**特点**：
- 按里程碑推进
- 不允许无边界扩需求
- 过程中可捕获隐性知识

---

## `/harness:review <change-id>`

**作用**：并行评审，重点看风险和规范。

会并行调用：
- 主评审（OpenSpec 对齐）
- 架构评审（Spring 分层/依赖）
- SQL 风险评审（全表更新、索引风险等）
- 评审摘要生成

---

## `/harness:archive <change-id>`

**作用**：把已完成变更归档到 `openspec/changes/archive/`。

**什么时候用**：Review 通过后。

---

## `/harness:knowledge`

**作用**：管理项目知识库。

常用子命令：

```text
/harness:knowledge
/harness:knowledge add
/harness:knowledge edit <id>
/harness:knowledge clean
/harness:knowledge export
```

---

## 4. 三个专项命令（评审增强）

## `/harness:prepare-review`

**作用**：生成“评审摘要”，适合提 PR 前快速说明变更范围和风险。

---

## `/harness:spring-architecture-review`

**作用**：检查 Spring 项目的分层和依赖方向。

典型检查：
- Controller 是否写了业务逻辑
- 是否有跨层乱调用
- 是否破坏分层边界

---

## `/harness:sql-risk-review`

**作用**：检查 SQL/Mapper 风险。

典型检查：
- 是否可能全表更新/删除
- 批量操作有没有条件限制
- 是否有索引/性能风险

---

## 5. 新手推荐操作顺序（照抄即可）

在 Cursor 对话里依次输入：

```text
/harness:setup
/harness:propose 用户登录
/harness:plan <上一步生成的change-id>
/harness:apply <change-id>
/harness:review <change-id>
/harness:archive <change-id>
```

---

## 6. 常见报错与解决

## 报错：`zsh: no such file or directory: /harness:setup`

**原因**：你在系统终端执行了 Slash 命令。

**解决**：去 Cursor 对话框里执行 `/harness:setup`。

---

## 报错：命令不生效

**排查**：
1. 是否刚安装后未重启会话
2. `.claude/settings.json` 是否存在 commands 配置
3. `.claude/skills/` 下是否有 `harness-setup` 等目录

---

## 7. 给小白的最后建议

- 先按流程走，不要跳步骤。
- 没有 `proposal/design/tasks` 就不要直接开写。
- 每次改完都做 review，再 archive。
- 把踩坑写进 knowledge，后面会越来越省时间。
